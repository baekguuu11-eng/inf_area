# v6.2.1 Patch 적용 방법

이 패치는 `INFECTED_AREA_v6_2_VisibilityGameplayUpgrade` 기준입니다.

1. Unity를 완전히 종료합니다.
2. 이 ZIP의 `Assets` 폴더를 현재 프로젝트 최상위 폴더에 복사합니다.
3. 같은 파일 덮어쓰기를 허용합니다.
4. Unity `6000.4.1f1`로 다시 열고 Compile을 기다립니다.
5. Console 빨간 오류를 확인한 뒤 GameScene을 실행합니다.

전체 프로젝트 ZIP을 사용하는 경우에는 이 패치를 따로 적용하지 않아도 됩니다.
